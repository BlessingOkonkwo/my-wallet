import React from 'react';

type Props = {
    desc: string;
    showDialog: () => void;
}

const FundButton: React.FC<Props> = (props) => {
  return (
    <button>
      
    </button>
  )
}

export default FundButton
